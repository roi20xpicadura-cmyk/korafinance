project_id = "chkgnqrfrtovcpqwogeg"

[functions.process-email-queue]
verify_jwt = true

[functions.send-whatsapp-notifications]
verify_jwt = false

[functions.hotmart-webhook]
verify_jwt = false

[functions.platform-webhook]
verify_jwt = false

[functions.whatsapp-webhook]
# Deprecated — replaced by `wpp-webhook` because the original function's
# gateway kept enforcing JWT even after config.toml set this to false.
# Remove this entry and delete the function directory once the Z-API
# dashboard is pointing to the new URL.
verify_jwt = false

[functions.wpp-webhook]
# Z-API webhooks don't carry a Supabase JWT; the function authenticates
# via message dedup + (optionally) a secret in the URL path/query.
verify_jwt = false
